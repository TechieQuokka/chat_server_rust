apiVersion: 1

providers:
  - name: 'Chat Server Dashboards'
    orgId: 1
    folder: 'Chat Server'
    folderUid: 'chat-server'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
